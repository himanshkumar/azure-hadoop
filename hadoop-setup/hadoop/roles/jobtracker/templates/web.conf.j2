<VirtualHost _default_:80>
  DocumentRoot "/var/www/pub"
  RewriteEngine On
  # Redirect all requests to the local Apache server to port 50030
  RewriteRule ^.*$ http://%{HTTP_HOST}:50030%{REQUEST_URI}
</VirtualHost>
